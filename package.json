{
  "version": "0.0.0",
  "private": true,
  "name": "test",
  "description": "font end task",
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-concat": "^0.5.1",
    "grunt-contrib-jshint": "^0.11.2",
    "grunt-contrib-uglify": "^0.9.1"
  }
}
