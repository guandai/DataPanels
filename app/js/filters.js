'use strict';
define([
    'angular'
], function(angular) {
    /* Filters */

    // not used yet
    angular.module('widgetFilters', []).factory('translate', function() {
        return function(input) {
            return input
        };
    })

});
